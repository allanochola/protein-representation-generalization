# Results — Experiment 01 (secondary structure)

Pending run. Against the frozen bands in `PROTOCOL.md`, this will report:

- **Δ_ESM(divergent)** with a cluster-bootstrap CI — ESM's Q3 margin over the ±3 local-sequence baseline on divergent proteins.
- **G** with a cluster-bootstrap CI — the Q3 drop from the in-distribution (random) test to the divergent test.
- Q3 accuracy and macro-F1 for ESM and the local baseline on both the in-distribution and divergent tests.
- The verdict: H_repr / H_shortcut / unresolved, applied mechanically from the frozen bands.

No numbers appear here until the run completes; the bands are fixed and are not moved to fit the result.
